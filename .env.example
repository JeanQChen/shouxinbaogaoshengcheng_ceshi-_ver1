# 复制本文件为 .env 并填入真实值。.env 已被 .gitignore 忽略，切勿提交。

# 外部搜索提供方。Phase 3 唯一启用博查（bocha），请保持为 bocha。
# 不读取 / 不要求 / 不检查 TAVILY_API_KEY；Tavily 不参与运行时、fallback 或验收。
EXTERNAL_SEARCH_PROVIDER=bocha

# 博查 Web Search API Key（必填）。
BOCHA_API_KEY=
